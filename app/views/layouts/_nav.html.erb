<ul class="stack-for-small sub-nav">
  <% @page_nav.main_nav.each do |page| %>
		<dd><%= nav_link_to(page.name.upcase, page_path(page.permalink), {}, { selected_class: "active", wrapper: "dd" }) %></dd>
  <% end %>
</ul>
